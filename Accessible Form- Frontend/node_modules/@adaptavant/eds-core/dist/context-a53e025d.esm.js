'use client';
import { useContext, createContext } from 'react';
import { jsx } from 'react/jsx-runtime';

const FocusRefContext = /*#__PURE__*/createContext(undefined);

/**
 * Context provider with a ref to the focusable element.
 * Adornments can use this ref to programmatically focus the element.
 */
function FocusRefContextProvider({
  children,
  value
}) {
  return /*#__PURE__*/jsx(FocusRefContext.Provider, {
    value: value,
    children: children
  });
}

/**
 * Hook to get the ref to the focusable element.
 */
function useFormControlRef() {
  const context = useContext(FocusRefContext);
  if (!context) {
    throw new Error('useFormControlRef must be used within a FormControlRefContextProvider');
  }
  return context;
}

export { FocusRefContextProvider, useFormControlRef };
