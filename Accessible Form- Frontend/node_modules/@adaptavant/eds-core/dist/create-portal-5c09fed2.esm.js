'use client';
import { createContext, useContext } from 'react';

const CreatePortalContext = /*#__PURE__*/createContext(undefined);
const CreatePortalProvider = CreatePortalContext.Provider;

/** @private */
function useCreatePortal() {
  const context = useContext(CreatePortalContext);
  if (!context) {
    throw new Error('usePortal must be used within a PortalProvider');
  }
  return context;
}

export { CreatePortalProvider, useCreatePortal };
