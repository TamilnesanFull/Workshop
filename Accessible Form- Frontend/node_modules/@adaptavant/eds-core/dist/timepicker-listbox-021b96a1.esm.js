'use client';
import { useEffect } from 'react';
import { useDropdownContext } from './context-6e374e37.esm.js';
import { a as forwardRefWithGenericComponent } from './forward-ref-a0e1d5d1.esm.js';
import { useComboboxListbox } from './combobox-listbox-c2913f04.esm.js';
import { jsx } from 'react/jsx-runtime';
import { Listbox } from './listbox-279326e9.esm.js';

const TimePickerListbox = forwardRefWithGenericComponent(function TimePickerListbox(props, forwardedRef) {
  const {
    listboxProps
  } = useComboboxListbox(props, forwardedRef);
  const {
    'data-highlighted-index': highlightedIndex
  } = props;
  const {
    dispatch
  } = useDropdownContext();
  useEffect(() => {
    dispatch({
      type: 'SET_HIGHLIGHTED_INDEX',
      payload: {
        activeIndex: highlightedIndex
      }
    });
  }, [dispatch, highlightedIndex]);
  return /*#__PURE__*/jsx(Listbox, {
    ...listboxProps
  });
});

export { TimePickerListbox };
