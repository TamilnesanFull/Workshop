'use strict';

var tw = require('./tw-cabb7e4c.cjs.dev.js');

// ----------------------------- ComboboxListbox -----------------------------

const getComboboxListboxStyles = () => ({
  root: tw.tw`
		outline-none
		overflow-y-auto
	`
});

// ----------------------------- ComboboxItemStyles -----------------------------

const getComboboxItemStyles = selectedOption => ({
  root: tw.tw`
		${selectedOption ? 'px-1' : 'px-2'}
		gap-1
		items-start
	`
});

exports.getComboboxItemStyles = getComboboxItemStyles;
exports.getComboboxListboxStyles = getComboboxListboxStyles;
