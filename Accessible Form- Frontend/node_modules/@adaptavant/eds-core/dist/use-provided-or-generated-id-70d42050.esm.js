'use client';
import { useId } from 'react';

/** Generate a unique ID if one is not provided. */
function useProvidedOrGeneratedId(id) {
  const generatedId = useId();
  return id || generatedId;
}

export { useProvidedOrGeneratedId };
