// -------------------------------- TimePicker ---------------------------------

/**
 * Parts of the TimePicker component that can be styled.
 */

/** Represents each option in the TimePicker and the data sent to the callback. */

/** Represents the state of the TimePicker input value. */
let TimePickerInputState = /*#__PURE__*/function (TimePickerInputState) {
  TimePickerInputState["EMPTY"] = "EMPTY";
  TimePickerInputState["VALID"] = "VALID";
  TimePickerInputState["INVALID_FORMAT"] = "INVALID_FORMAT";
  TimePickerInputState["LTE_MIN"] = "LTE_MIN";
  TimePickerInputState["GTE_MAX"] = "GTE_MAX";
  return TimePickerInputState;
}({});

// ------------------------------ TimePickerInput ------------------------------

// ----------------------------- TimePickerListbox -----------------------------

export { TimePickerInputState as T };
