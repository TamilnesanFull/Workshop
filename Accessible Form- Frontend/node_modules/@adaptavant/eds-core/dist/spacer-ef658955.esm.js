import { g as getStyleProps } from './index-dd10a77e.esm.js';
import { jsx } from 'react/jsx-runtime';
import { B as Box } from './box-a5dda214.esm.js';

/**
 * Spacer
 *
 * @description An unstyled, empty element that can have its width & height set to mimic that of another element, like an icon.
 */

function Spacer({
  className = '',
  classNames = {},
  style = {},
  styles = {},
  ...consumerProps
}) {
  const styleProps = getStyleProps({
    className,
    classNames,
    style,
    styles,
    tw: {
      root: 'inline-block'
    }
  });
  return /*#__PURE__*/jsx(Box, {
    as: "span",
    ...consumerProps,
    ...styleProps('root')
  });
}
Spacer.displayName = 'Spacer';

export { Spacer as S };
