/**
 * Compose multiple functions into a single function. The functions will be
 * called in the order they are passed in.
 */
export declare function chainFunctions<Func extends AnyFunction>(...fns: Func[]): (...args: Parameters<Func[][number]>) => void;
type AnyFunction = (...args: any[]) => unknown;
export {};
