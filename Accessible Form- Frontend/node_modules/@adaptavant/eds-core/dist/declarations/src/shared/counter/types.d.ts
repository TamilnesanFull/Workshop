import { type StyleProps } from "../style-utils/index.js";
type CounterParts = 'root';
/**
 * Counter object containing maxValue and value.
 * "maxValue:" maximum value the counter can reach.
 * "value:" current value of the counter.
 * @example { value: 10, maxValue: 15, isAlwaysVisible: true }
 */
export type CounterObjProps = {
    /** maximum value the counter can reach. */
    maxValue: number;
    /** current value of the counter. */
    value: number;
    /** controls the visibility of the counter
     * @default false
     */
    isAlwaysVisible: boolean;
};
export type CounterProps = CounterObjProps & Partial<StyleProps<CounterParts>>;
export type GetCounterStyles = (args: CounterProps) => Partial<Record<CounterParts, string>>;
type CounterCircleParts = 'root' | 'backCircle' | 'frontCircle';
export type CounterCircleProps = Pick<CounterObjProps, 'value' | 'maxValue'> & Partial<StyleProps<CounterCircleParts>>;
export type GetCounterCircleStyles = () => Partial<Record<CounterCircleParts, string>>;
export {};
