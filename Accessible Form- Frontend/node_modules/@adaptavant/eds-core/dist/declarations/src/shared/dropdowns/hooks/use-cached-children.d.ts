export type CachedChildrenOptions<Option extends object> = {
    /**
     * Defines the content to be rendered within the collection.
     *
     * Accepts either a ReactNode or a function that returns a ReactNode for each
     * item in the collection.
     * If a function is provided, its result for each item is cached to optimise
     * re-renders.
     */
    children?: React.ReactNode | ((option: Option) => React.ReactNode);
    /**
     * An array of items that the collection should render.
     *
     * Note: This is mandatory if `children` is a function.
     */
    options?: ReadonlyArray<Option>;
};
/**
 * A simplified version of `useCachedChildren` adapted from
 `react-aria-components`.
 *
 * @see https://github.com/adobe/react-spectrum/blob/main/packages/react-aria-components/src/Collection.tsx#L663
 */
export declare function useCachedChildren<Option extends object>({ children, options, }: CachedChildrenOptions<Option>): import("react").ReactNode;
