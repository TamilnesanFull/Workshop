import { type SnackbarProps } from "./types.js";
/** Snackbar component helps to display temporary notifications of actions, errors or other events.
 * @description This component extends the `SnackbarContent` component and has the ability to control the visibility, position & auto-hide features of the Snackbar.
 */
export declare const Snackbar: {
    ({ className, classNames, styles, title, description, autoCloseDuration, variant, placement, open, offset, onClose, onCloseButtonClick, actionButton, ...consumerProps }: SnackbarProps): false | import("react").ReactPortal | null;
    displayName: string;
};
