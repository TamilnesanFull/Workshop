import { type TrackParts, type TrackProps } from "../../components/track/types.js";
import { type ForwardRefComponent } from "../../shared/types.js";
export type MenuItemProps = TrackProps & {
    /**
     * Indicates if the menu item is currently disabled. Used for styling and accessibility.
     * Applies a aria-disabled attribute.
     *
     * @default false
     */
    isDisabled?: boolean;
    /**
     * Indicates if the menu item is currently highlighted. Used for styling.
     * Applies a data-highlighted attribute.
     *
     * @default false
     */
    isHighlighted?: boolean;
    /**
     * Indicates if the menu item is currently selected.  Used for styling.
     * Applies a data-selected attribute.
     *
     * @default false
     */
    isSelected?: boolean;
    /**
     * The size of the menu item.
     *
     * @default 'standard'
     */
    size?: 'standard' | 'large';
};
export type PolymorphicMenuItem = ForwardRefComponent<'div', MenuItemProps>;
export type GetMenuItemStyles = ({ size, }: Required<Pick<MenuItemProps, 'size'>>) => Partial<Record<TrackParts, string>>;
