import { type StyleProps } from "../../shared/index.js";
import { type CachedChildrenOptions } from "../../shared/dropdowns/hooks/use-cached-children.js";
type Parts = 'root';
export type ListboxProps<Option extends object> = Partial<StyleProps<Parts>> & CachedChildrenOptions<Option> & {
    /** Unique identifier for the listbox. */
    id: string;
};
export {};
