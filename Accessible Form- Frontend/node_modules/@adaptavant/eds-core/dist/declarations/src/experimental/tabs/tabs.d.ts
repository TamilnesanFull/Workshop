import { type TabsProps } from "./types.js";
/**
 * Tabs component represents a set of tabs that allows users to switch between different content views.
 */
export declare function Tabs({ children, className, classNames, defaultValue, onSelectedValueChange, selectedValue, style, styles, size, ...consumerProps }: TabsProps): import("react/jsx-runtime").JSX.Element;
export declare namespace Tabs {
    var displayName: string;
}
