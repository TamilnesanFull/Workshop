import { type DialogHeaderProps } from "./types.js";
/**
 * Should be added as first child of a Dialog.
 * - Compound component can accommodate title, subheading, Logo(if any) of Dialog
 * - Will render close button at right end when `closeButtonProps` is passed
 */
export declare const DialogHeader: {
    ({ children, className, classNames, closeButtonProps, style, styles, ...consumerProps }: DialogHeaderProps): import("react/jsx-runtime").JSX.Element;
    displayName: string;
};
