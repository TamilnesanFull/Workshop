import { type StyleProps } from "../../shared/style-utils/index.js";
type Parts = 'root' | 'icon';
export type AlertProps = Partial<StyleProps<Parts>> & {
    children: React.ReactNode;
    /** The variant of the Alert.
     * @default 'info'
     */
    variant?: 'caution' | 'critical' | 'info' | 'positive';
};
export type GetAlertStyles = (props: Required<Pick<AlertProps, 'variant'>>) => Partial<Record<Parts, string>>;
export {};
