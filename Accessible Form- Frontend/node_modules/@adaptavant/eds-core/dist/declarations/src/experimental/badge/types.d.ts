import { type IconProps } from "../../components/icon/types.js";
import { type StyleProps } from "../../shared/style-utils/index.js";
/**
 * Parts of the Badge component that can be styled.
 */
type Parts = 'root' | 'iconStart' | 'label';
export type BadgeProps = Partial<StyleProps<Parts>> & {
    /** The children will be displayed as badge content. */
    children: React.ReactNode;
    /** The size of the badge. */
    size?: 'standard' | 'small';
    /** The tone or color variant of the badge. */
    tone?: 'positive' | 'neutral' | 'caution' | 'critical';
    /** The icon to display before the badge children. */
    iconStart?: (props: IconProps) => React.ReactNode;
};
export type GetBadgeStyles = (props: Required<Pick<BadgeProps, 'tone' | 'size'>>) => Partial<Record<Parts, string>>;
export {};
