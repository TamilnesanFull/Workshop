import { type StyleProps } from "../../shared/style-utils/index.js";
import { type ForwardRefComponent } from "../../shared/types.js";
type Parts = 'root' | 'child';
export type AlignChildToTextProps = {
    /** The content that you want to vertically align with text. */
    children: React.ReactNode;
} & Partial<StyleProps<Parts>>;
export type PolymorphicAlignChildToText = ForwardRefComponent<'span', AlignChildToTextProps>;
export {};
