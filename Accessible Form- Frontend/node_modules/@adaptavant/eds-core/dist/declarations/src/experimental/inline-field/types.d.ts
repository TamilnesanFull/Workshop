import { type LabelProps } from "../../components/label/index.js";
import { type CounterObjProps } from "../../shared/counter/types.js";
import { type StyleProps } from "../../shared/style-utils/index.js";
type Parts = 'root' | 'description' | 'errorIcon' | 'errorMessage' | 'errorTrack' | 'inputWrapper' | 'labelWrapper' | 'label' | 'secondaryLabel' | 'counter';
export type InlineFieldProps = Partial<StyleProps<Parts>> & Omit<LabelProps, 'children' | 'size'> & {
    /** Input component. */
    children: React.ReactNode;
    /** Message to show when the field is invalid. */
    errorMessage?: React.ReactNode;
    /** Sets a unique identifier for the component. */
    id?: string;
    /**
     * Whether the field is disabled.
     * @default false
     */
    isDisabled?: boolean;
    /**
     * Whether user input is required on the field before form submission.
     * @default false
     */
    isRequired?: boolean;
    /** Label for the field. */
    label: React.ReactNode;
    /**
     * Counter object containing maxValue and value.
     * "maxValue:" maximum value the counter can reach.
     * "value:" current value of the counter.
     * @example { value: 10, maxValue: 15 }
     */
    counter?: CounterObjProps;
};
export type GetInlineFieldStyles = () => Partial<Record<Parts, string>>;
export {};
