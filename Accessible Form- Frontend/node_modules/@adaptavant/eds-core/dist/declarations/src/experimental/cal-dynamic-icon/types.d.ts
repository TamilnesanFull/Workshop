import { type IconProps } from "../../components/icon/types.js";
export type CalDynamicIconProps = Omit<IconProps, 'tone'> & {
    /** The content inside the SVG, either a string or a number. */
    children: string | number;
    /** The visual style of the CalDynamicIcon
     * @default standard
     */
    variant?: 'standard' | 'filled';
};
