import { type FieldProps } from "../../components/field/index.js";
import { type TextInputProps } from "../../components/text-input/index.js";
export type Parts = 'root' | 'adornmentStart' | 'adornmentEnd';
/** @deprecated use TextInputProps instead. */
export type GhostInputProps = TextInputProps;
export type GetGhostInputStyles = (props: Required<Pick<FieldProps, 'size'>> & {
    hasAdornmentStart: boolean;
    hasAdornmentEnd: boolean;
    isInvalid: boolean;
}) => Partial<Record<Parts, string>>;
