import { type PolymorphicAlignChildToText } from "./types.js";
/**
 * AlignChildToText
 *
 * @description a component that uses a zero-width space to vertically align a
 * child element such as an icon with a line of text. This is especially useful
 * when text needs to wrap.
 *
 * @example
 * <Text>
 * 	<AlignChildToText>
 * 		<MapPinIcon />
 * 	</AlignChildToText>
 * 	{address}
 * </Text>
 */
export declare const AlignChildToText: PolymorphicAlignChildToText;
