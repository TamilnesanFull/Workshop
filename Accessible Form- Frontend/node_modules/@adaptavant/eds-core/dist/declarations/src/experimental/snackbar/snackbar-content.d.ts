import { type SnackbarContentProps } from "./types.js";
/**
 * The SnackbarContent component can used as a standalone primitive to build a user feedback system.
 *
 */
export declare const SnackbarContent: {
    ({ className, classNames, style, styles, variant, onCloseButtonClick, title, description, actionButton, ...consumerProps }: SnackbarContentProps): import("react/jsx-runtime").JSX.Element;
    displayName: string;
};
