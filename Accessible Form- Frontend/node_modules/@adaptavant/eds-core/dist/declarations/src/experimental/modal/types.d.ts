import { type MobileDropdownContextType } from "../../shared/dropdowns/mobile-context.js";
import { type StyleProps } from "../../shared/style-utils/index.js";
import { type DialogBaseHeaderProps, type DialogBaseProps, type DialogCloseButtonProps, type DialogContentProps, type DialogFooterProps } from "../dialog/types.js";
type Parts = 'root' | 'modalWrapper';
export type ModalProps = Partial<StyleProps<Parts>> & Omit<DialogBaseProps, 'delay'> & {
    /** Indicates if the modal should be render Sheet in responsive view
     * @default true
     */
    mobileFriendly?: boolean;
    /**
     * The size of the Modal 'Dialog' box in pixels.
     * When passed, the CSS `width` property will be set accordingly.
     *
     * @default 640
     * @example
     * // Example usage:
     * // Sets the modal size to 800 pixels
     * <Modal size={800} />
     */
    size?: number;
} & Pick<MobileDropdownContextType, 'togglePoint'>;
export type GetModalStyles = () => Partial<Record<Parts, string>>;
export type ModalHeaderProps = DialogBaseHeaderProps & {
    closeButtonProps?: Omit<DialogCloseButtonProps, 'size'>;
};
export type ModalContentProps = DialogContentProps;
export type ModalFooterProps = DialogFooterProps;
export {};
