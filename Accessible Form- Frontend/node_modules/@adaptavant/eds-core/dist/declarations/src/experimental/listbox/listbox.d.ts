/**
 * @private
 * Listbox
 *
 * @description
 * Used for lists from which a user can select one or more items.
 *
 * Assigns a role of 'listbox' for accessibility purposes.
 * If `options` are provided, children can be a function that we can call to
 * render the options.
 *
 * @example
 * <Listbox options={options}>
 * 	{(option) => (
 * 		<div aria-selected={isSelected} role="option">
 * 			{option.label}
 * 		</div>
 * 	)}
 * </Listbox>
 */
export declare const Listbox: <Option extends object>(props: Partial<import("../../index.js").Unstable_StyleProps<"root">> & import("../../shared/dropdowns/hooks/use-cached-children.js").CachedChildrenOptions<Option> & {
    id: string;
} & import("react").RefAttributes<HTMLDivElement>) => JSX.Element;
