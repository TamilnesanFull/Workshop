import { type StyleProps } from "../../shared/style-utils/index.js";
type Parts = 'root' | 'baseArea' | 'filledArea' | 'adornmentStart' | 'adornmentEnd';
export type ProgressProps = Partial<StyleProps<Parts>> & {
    /** Slot to render end adornment. */
    adornmentEnd?: React.ReactNode;
    /** Slot to render start adornment. */
    adornmentStart?: React.ReactNode;
    /**
     * The `id` of the element that serves as the accessible label for the progressbar.
     * This value is used to set the `aria-labelledby` attribute on the progressbar, linking it to a visible or hidden label element.
     *
     * If no visible label is associated with the progress, consider providing a <Label /> component with the `sr-only` class
     * to ensure accessibility for screen readers.
     *
     */
    ariaLabelledBy: string;
    /** Maximum value the Progress can reach.
     *
     * @default 100
     */
    maxValue?: number;
    /** Current value of the Progress.
     *
     * @default 0
     */
    value?: number;
};
export type GetProgressStyles = () => Partial<Record<Parts, string>>;
export {};
