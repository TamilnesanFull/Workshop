import { type StyleProps } from "../../shared/style-utils/index.js";
type Parts = 'root' | 'iconAfterAnimation' | 'iconBeforeAnimation' | 'iconWrapper' | 'label';
export type GetProProps = Partial<StyleProps<Parts>> & {
    /**
     * Whether or not show 'Get Pro' text on default view.
     * When false, 'aria-label' as "Get Pro" will be announced to screen-readers.
     * @default 'false'
     */
    isExpanded?: boolean;
    /**
     * The size of the "GetPro" element.
     * @default 'standard'
     */
    size?: 'small' | 'standard' | 'large';
    /**
     * Whether or not show 'GetPro' element styled like button (circular radius).
     * Use it along with `isExpanded`.
     * @default 'false'
     */
    isRounded?: boolean;
    /**
     * Textcontent of the "GetPro" element.
     * @default 'Get Pro'
     */
    label?: string;
};
export type GetGetProStyles = (props: Required<Pick<GetProProps, 'isRounded' | 'size' | 'isExpanded'>>) => Partial<Record<Parts, string>>;
export {};
