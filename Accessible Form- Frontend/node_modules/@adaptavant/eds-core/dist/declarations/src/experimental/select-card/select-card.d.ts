import { type SelectCardProps } from "./types.js";
/**
 * A SelectCard is a controlled component that allows users to select one or more options from a set of options.
 * It is useful for presenting a list of options in a card-like format.
 *
 * @example
 * const options = [
 *	{
 *		value: 'option1',
 *		directSlot: <div>Direct slot content for Option 1</div>,
 *	},
 *	{
 *		value: 'option2',
 *		directSlot: <div>Direct slot content for Option 2</div>,
 *		dynamicSlot: <div>Dynamic slot content for Option 2</div>
 *	}
 * ];
 *
 * const [selectedOptions, setSelectedOptions] = React.useState<string[]>([]);
 *
 * return (
 *  <SelectCard
 *    options={options}
 *    value={selectedOptions}
 *    onChange={(newSelectedOptions) => setSelectedOptions(newSelectedOptions)}
 *  />
 * );
 *
 * @property controlType - The type of control to render.
 * @property options - The options to render.
 * @property value - The selected value(s).
 * @property onchange - The function to call when the value changes.
 *
 * Above are mandatory props, to see other optional props please check our prop table API page in documentation.
 */
export declare function SelectCard({ className, classNames, controlType, isDisabled, legend, onChange, options, orientation, style, styles, value, ...consumerProps }: SelectCardProps): import("react/jsx-runtime").JSX.Element;
