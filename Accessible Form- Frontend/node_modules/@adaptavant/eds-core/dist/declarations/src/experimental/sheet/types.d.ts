import { type StyleProps } from "../../shared/style-utils/index.js";
import { type DialogBaseHeaderProps, type DialogBaseProps, type DialogCloseButtonProps, type DialogContentProps, type DialogFooterProps } from "../dialog/types.js";
type Parts = 'root' | 'sheetWrapper';
export type SheetProps = Partial<StyleProps<Parts>> & Omit<DialogBaseProps, 'delay'> & {
    /** Sheet component placement */
    placement?: 'left' | 'right' | 'top' | 'bottom';
};
export type GetSheetStyles = (props: Required<Pick<SheetProps, 'placement' | 'open'>>) => Partial<Record<Parts, string>>;
export type SheetHeaderProps = DialogBaseHeaderProps & {
    closeButtonProps?: Omit<DialogCloseButtonProps, 'size'>;
};
export type SheetContentProps = DialogContentProps;
export type SheetFooterProps = DialogFooterProps;
export {};
