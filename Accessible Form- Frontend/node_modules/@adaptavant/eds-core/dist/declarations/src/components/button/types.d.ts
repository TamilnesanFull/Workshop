import { type StyleProps } from "../../shared/style-utils/index.js";
import { type BrandStyles } from "../../shared/types.js";
import { type IconProps } from "../icon/index.js";
type Parts = 'root' | 'labelTrack' | 'label' | 'iconStart' | 'iconEnd' | 'loadingWrapper' | 'loading';
export type ButtonProps = Pick<React.ButtonHTMLAttributes<HTMLButtonElement>, 'aria-controls' | 'aria-expanded' | 'aria-haspopup' | 'children' | 'form' | 'id' | 'onBlur' | 'onClick' | 'onFocus' | 'onKeyDown' | 'type'> & {
    /** The icon to display before the buttons children. */
    iconStart?: (props: IconProps) => React.ReactNode;
    /** The icon to display after the buttons children. */
    iconEnd?: (props: IconProps) => React.ReactNode;
    /** When true, the button will be announced as disabled to screen-readers only. */
    isDisabled?: boolean;
    /** When true, the button will display a loading indicator. */
    isLoading?: boolean;
    /** When true, the button will have aria-pressed set and be styled visually pressed. */
    isPressed?: boolean;
    /** Text to read out to assistive technologies when button is loading. */
    loadingLabel?: string;
    /** The size of the button. */
    size?: 'large' | 'standard' | 'small';
    /** The variant of the button. */
    variant?: 'accentPrimary' | 'accentSecondary' | 'criticalPrimary' | 'criticalTertiary' | 'neutralPrimary' | 'neutralSecondary' | 'neutralSecondaryIntense' | 'neutralTertiary';
} & Partial<StyleProps<Parts>>;
export type GetButtonStyles = (props: Pick<BrandStyles, 'buttonRadius'> & Required<Pick<ButtonProps, 'isDisabled' | 'isLoading' | 'isPressed' | 'size' | 'variant'>>) => Partial<Record<Parts, string>>;
export {};
