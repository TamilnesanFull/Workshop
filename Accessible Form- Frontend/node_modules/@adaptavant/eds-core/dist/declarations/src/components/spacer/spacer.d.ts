import { type SpacerProps } from "./types.js";
/**
 * Spacer
 *
 * @description An unstyled, empty element that can have its width & height set to mimic that of another element, like an icon.
 */
export declare function Spacer({ className, classNames, style, styles, ...consumerProps }: SpacerProps): import("react/jsx-runtime").JSX.Element;
export declare namespace Spacer {
    var displayName: string;
}
