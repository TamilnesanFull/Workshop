import { type ButtonProps } from "./types.js";
type UseButtonProps = Required<Pick<ButtonProps, 'isDisabled' | 'isLoading'>> & Pick<ButtonProps, 'isPressed' | 'onClick'>;
/**
 * Provides button-specific properties and handles click events, considering
 * loading and disabled states.
 */
export declare function useButton({ isDisabled, isLoading, isPressed, onClick }: UseButtonProps, forwardedRef: React.Ref<HTMLButtonElement>): {
    buttonProps: {
        'aria-disabled': true | undefined;
        'aria-pressed': boolean | undefined;
        onClick: (event: React.MouseEvent<HTMLButtonElement, MouseEvent>) => void;
        ref: import("react").ForwardedRef<HTMLButtonElement>;
    };
};
export {};
