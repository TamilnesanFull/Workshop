import { type FieldProps } from "./types.js";
/**
 * Field
 *
 * @description
 * The Field component exposes the elements around form inputs, and an API to
 * compose them.
 */
export declare function Field({ children, className, classNames, controlId: maybeControlId, counter, description, errorMessage, isDisabled, isRequired, label, labelVisibility, secondaryLabel, size, style, styles, ...consumerProps }: FieldProps): import("react/jsx-runtime").JSX.Element;
export declare namespace Field {
    var displayName: string;
}
export declare function useFieldIds(id: string | undefined): {
    controlId: string;
    descriptionId: string;
    errorMessageId: string;
    labelId: string;
};
