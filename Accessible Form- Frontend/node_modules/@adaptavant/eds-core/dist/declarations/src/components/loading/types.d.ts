import { type StyleProps } from "../../shared/style-utils/index.js";
type Parts = 'root' | 'icon' | 'circle' | 'circle-path';
export type LoadingProps = Pick<React.SVGProps<HTMLDivElement>, 'id'> & {
    /** The size of the icon.
     * @default 24
     */
    size?: '16' | '20' | '24' | '48' | '80';
    /** The tone of the icon.
     * @default accentPrimary
     */
    tone?: 'accentPrimary' | 'neutralPrimary' | 'onPrimary' | 'criticalPrimary' | 'critical-onPrimary' | 'inverse';
    /** The icon to display inside the Loading.
     * @default growYourBrandIcon
     */
    icon?: 'growYourBrandIcon' | 'none' | 'setmoreIcon';
} & Partial<StyleProps<Parts>>;
export type GetLoadingStyles = (props: Pick<LoadingProps, 'tone'> & Required<Pick<LoadingProps, 'size'>>) => Partial<Record<Parts, string>>;
export {};
