import { type StyleProps } from "../../shared/style-utils/index.js";
type Parts = 'root';
type NativeImageProps = React.ImgHTMLAttributes<HTMLImageElement>;
export type ImageProps = Pick<NativeImageProps, 'decoding' | 'height' | 'loading' | 'sizes' | 'srcSet' | 'width' | 'onError'> & {
    /** Defines an alternative text description of the image. */
    alt: string;
    /** The image URL. */
    src: string;
    /**
     * Controls how an image is resized to fit its container.
     *
     * @deprecated Please use object-fit utility classes instead.
     * @see https://tailwindcss.com/docs/object-fit
     */
    objectFit?: 'contain' | 'cover';
} & Partial<StyleProps<Parts>>;
export type GetImageStyles = (props: Pick<ImageProps, 'objectFit'>) => Partial<Record<Parts, string>>;
export {};
