import { type StyleProps } from "../../shared/style-utils/index.js";
import { type FieldProps } from "../field/index.js";
type Parts = 'root' | 'errorMessage' | 'errorIcon';
type FieldStyleProps = Pick<FieldProps, 'isDisabled' | 'size'>;
export type FieldErrorMessageProps = Partial<StyleProps<Parts>> & FieldStyleProps & {
    /** The error message to display. */
    children: React.ReactNode;
    /** Unique identifier for the error message. */
    id?: string;
    /**
     * Adds role="alert" when set to true.
     * This is useful for screen readers to announce the error message when it is first rendered.
     * @default true
     */
    shouldAnnounceOnMount?: boolean;
};
export type GetFieldErrorMessageStyles = (props: Required<FieldStyleProps>) => Partial<Record<Parts, string>>;
export {};
