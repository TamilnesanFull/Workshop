import { type BrandStyles, type Translations } from "../../shared/types.js";
import { type Tokens } from "../../tokens/index.js";
import { type Box } from "../box/box.js";
type BoxProps = React.ComponentPropsWithoutRef<typeof Box>;
export type RootProps = BoxProps & {
    brand: {
        tokens: Tokens;
        config: BrandStyles;
    };
    colorScheme: 'system' | 'light' | 'dark';
    translations: Translations;
};
export {};
