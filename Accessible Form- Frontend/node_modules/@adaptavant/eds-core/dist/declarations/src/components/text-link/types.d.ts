import { type StyleProps } from "../../shared/style-utils/index.js";
type Parts = 'root';
export type TextLinkProps = Pick<React.AnchorHTMLAttributes<HTMLAnchorElement>, 'target' | 'rel'> & {
    /**
     * The variant of the TextLink.
     */
    variant?: 'accentPrimary' | 'neutralPrimary' | 'neutralSecondary' | 'regular';
    /**
     * The content to display in the link.
     */
    children: React.ReactNode;
    /**
     * A URL to link
     */
    href?: string;
} & Partial<StyleProps<Parts>>;
export type GetTextLinkStyles = (props: Required<Pick<TextLinkProps, 'variant'>>) => Partial<Record<Parts, string>>;
export {};
