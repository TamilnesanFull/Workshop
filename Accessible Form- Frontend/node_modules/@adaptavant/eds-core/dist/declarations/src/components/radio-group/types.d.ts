import { type StyleProps } from "../../shared/style-utils/index.js";
import { type FieldsetProps } from "../fieldset/index.js";
import { type RadioProps } from "../radio/index.js";
type Parts = 'root' | 'description' | 'legend' | 'errorMessage' | 'errorTrack' | 'errorIcon' | 'errorMessage' | 'wrapper';
export type RadioGroupProps<Value extends string = string> = Pick<FieldsetProps, 'children' | 'description' | 'errorMessage' | 'id' | 'isRequired' | 'legend' | 'orientation'> & {
    /** Whether or not the radio group is disabled. */
    isDisabled?: boolean | ((value: Value) => boolean);
    /** Name for the group of `Radio` components. */
    name: string;
    /**
     * Function that is called whenever a blur event is triggered on a `Radio`.
     */
    onBlur?: (selectedValue: Value) => void;
    /**
     * Function that is called whenever a change event is triggered on a `Radio`.
     */
    onChange: (selectedValue: Value) => void;
    /** The size of the radio. */
    size?: RadioProps['size'];
    /** The current value. */
    value: Value;
} & Partial<StyleProps<Parts>>;
export {};
