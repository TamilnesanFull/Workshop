import { type StyleProps } from "../../shared/style-utils/index.js";
import { type BrandStyles } from "../../shared/types.js";
import { type ButtonProps } from "../button/index.js";
type Parts = 'root';
export type ChipProps = Partial<StyleProps<Parts>> & Pick<ButtonProps, 'aria-controls' | 'aria-expanded' | 'aria-haspopup' | 'children' | 'id' | 'isPressed' | 'onClick' | 'size'>;
export type GetChipStyles = (props: Pick<BrandStyles, 'buttonRadius'> & Required<Pick<ChipProps, 'isPressed' | 'size'>>) => Partial<Record<Parts, string>>;
export {};
