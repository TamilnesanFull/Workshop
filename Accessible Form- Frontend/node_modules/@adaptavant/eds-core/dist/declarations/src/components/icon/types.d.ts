import { type StyleProps } from "../../shared/style-utils/index.js";
type Parts = 'root';
type NativeSvgProps = React.SVGProps<SVGSVGElement>;
export type IconProps = Partial<StyleProps<Parts>> & Pick<NativeSvgProps, 'aria-hidden' | 'id'> & {
    /**
     * The size of the icon.
     * @default '24'
     * */
    size?: '16' | '20' | '24' | '32' | '40';
    /** The tone of the icon. */
    tone?: 'accentPrimary' | 'criticalPrimary' | 'neutralPrimary' | 'neutralSecondary' | 'neutralTertiary';
};
export type GetIconStyles = (props: Pick<IconProps, 'tone'> & Required<Pick<IconProps, 'size'>>) => Partial<Record<Parts, string>>;
export {};
