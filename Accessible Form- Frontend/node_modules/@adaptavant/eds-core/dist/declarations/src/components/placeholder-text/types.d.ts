import { type StyleProps } from "../../shared/style-utils/index.js";
type Parts = 'root';
export type PlaceholderTextProps = {
    /**
     * The number of approximate characters to render.
     */
    characters: number;
} & Partial<StyleProps<Parts>>;
export {};
