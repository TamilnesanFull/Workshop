import { type CropDimensions } from "../types.js";
type SetupCropAreaParams = {
    canvasHeight: number;
    canvasWidth: number;
    cropDimensions: CropDimensions | null;
    cropHeight: number;
    cropWidth: number;
    cropperInstance: Cropper;
    maxScaleFactor: number;
    setZoom: (zoom: number) => void;
    setMinZoom: (minZoom: number) => void;
};
export declare const setupCropperArea: ({ canvasHeight, canvasWidth, cropDimensions, cropHeight, cropperInstance, cropWidth, maxScaleFactor, setZoom, setMinZoom, }: SetupCropAreaParams) => void;
export {};
