import { type OnError } from "../index.js";
type ResizeImageOptions = {
    onError?: OnError;
    targetWidth: number;
    targetHeight: number;
    rescaleWhenSmaller?: boolean;
};
export declare const resizeImage: (blob: Blob, options?: Partial<ResizeImageOptions>) => Promise<Blob>;
export {};
