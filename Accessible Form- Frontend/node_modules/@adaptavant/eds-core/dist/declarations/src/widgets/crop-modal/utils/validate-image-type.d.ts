import { type OnError } from "../index.js";
export interface ValidateImageTypeResult {
    isValid: boolean;
    errorType?: 'mime' | 'read-android' | 'other' | null;
}
export declare const validateImageType: (file: File | null, acceptedMimeTypes: string[], onError?: OnError) => Promise<ValidateImageTypeResult>;
