import { type ImageProcessingOptions } from "../types.js";
export declare const DEFAULT_IMAGE_PROCESSING_OPTIONS: Required<ImageProcessingOptions>;
export declare const parseImageProcessingOptions: (imageProcessingOptions?: ImageProcessingOptions) => {
    allowedMimeType: string;
    maxWidth: number;
    maxHeight: number;
    enableResize: boolean;
    maxFileSizeMB: number;
    enableCompression: boolean;
    compressionQuality: number;
    allowedMimeTypes: string[];
};
