'use client';
import { createContext, useContext } from 'react';

/**
 * Provides the state passed into the RadioGroupContext provider.
 */
function useRadioGroupState({
  isDisabled = false,
  name,
  onChange,
  size,
  value
}) {
  return {
    isDisabled,
    name,
    onChange(event) {
      if (isDisabled === true) return;
      onChange(event.target.value);
    },
    size,
    value
  };
}
const RadioGroupContext = /*#__PURE__*/createContext(undefined);
const RadioGroupContextProvider = RadioGroupContext.Provider;
function useRadioGroupContext() {
  return useContext(RadioGroupContext);
}

export { RadioGroupContextProvider, useRadioGroupContext, useRadioGroupState };
