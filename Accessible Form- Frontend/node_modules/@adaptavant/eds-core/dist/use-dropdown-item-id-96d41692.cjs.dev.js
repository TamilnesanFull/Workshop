'use client';
'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var React = require('react');
var context = require('./context-1e651be3.cjs.dev.js');

/**
 * Generates the ID of the dropdown menu list item.
 *
 * The `baseId` prefix ensures that multiple dropdowns can be placed on the same
 * page.
 */
function useDropdownItemId(idProp) {
  const {
    baseId
  } = context.useDropdownContext();
  const id = React.useId();
  return idProp ?? `${baseId}-item-${id}`;
}

exports.useDropdownItemId = useDropdownItemId;
