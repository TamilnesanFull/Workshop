'use client';
import { useContext, createContext } from 'react';
import { jsx } from 'react/jsx-runtime';

const SelectMenuContext = /*#__PURE__*/createContext(undefined);
function SelectMenuProvider({
  children,
  value
}) {
  return /*#__PURE__*/jsx(SelectMenuContext.Provider, {
    value: value,
    children: children
  });
}
function useSelectMenuContext() {
  const context = useContext(SelectMenuContext);
  if (!context) {
    throw new Error('useSelectMenuContext must be used within a SelectMenuProvider');
  }
  return context;
}

export { SelectMenuProvider, useSelectMenuContext };
