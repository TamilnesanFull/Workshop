'use client';
import { useContext, createContext } from 'react';
import { jsx } from 'react/jsx-runtime';

const TranslationsContext = /*#__PURE__*/createContext(undefined);
const TranslationsProvider = ({
  children,
  value
}) => {
  // TODO: merge with parent
  // const parent = useContext(TranslationsContext);

  return /*#__PURE__*/jsx(TranslationsContext.Provider, {
    value: value,
    children: children
  });
};
function useTranslations() {
  const context = useContext(TranslationsContext);
  if (!context) {
    throw new Error('useTranslations must be used within a TranslationsProvider');
  }
  return context;
}

export { TranslationsProvider, useTranslations };
