'use client';
'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var React = require('react');
var jsxRuntime = require('react/jsx-runtime');

const FocusRefContext = /*#__PURE__*/React.createContext(undefined);

/**
 * Context provider with a ref to the focusable element.
 * Adornments can use this ref to programmatically focus the element.
 */
function FocusRefContextProvider({
  children,
  value
}) {
  return /*#__PURE__*/jsxRuntime.jsx(FocusRefContext.Provider, {
    value: value,
    children: children
  });
}

/**
 * Hook to get the ref to the focusable element.
 */
function useFormControlRef() {
  const context = React.useContext(FocusRefContext);
  if (!context) {
    throw new Error('useFormControlRef must be used within a FormControlRefContextProvider');
  }
  return context;
}

exports.FocusRefContextProvider = FocusRefContextProvider;
exports.useFormControlRef = useFormControlRef;
