import { forwardRef } from 'react';
import { g as getStyleProps } from './index-dd10a77e.esm.js';
import { jsx } from 'react/jsx-runtime';

/**
 * Stack
 *
 * @description Used to distribute children vertically, with even spacing between each child.
 */

const Stack = /*#__PURE__*/forwardRef(function Stack({
  as: Comp = 'div',
  className = '',
  classNames = {},
  style = {},
  styles = {},
  ...consumerProps
}, forwardedRef) {
  const styleProps = getStyleProps({
    className,
    classNames,
    style,
    styles,
    tw: {
      root: `
				flex
				flex-col
				items-start
			`
    }
  });
  return /*#__PURE__*/jsx(Comp, {
    ...consumerProps,
    ref: forwardedRef,
    ...styleProps('root'),
    "data-eds-component": true
  });
});
Stack.displayName = 'Stack';

export { Stack as S };
