'use client';
import { forwardRef } from 'react';
import { useComboboxPopover } from './combobox-popover-abd204b9.esm.js';
import { jsx } from 'react/jsx-runtime';
import { Popover } from './popover-55e45b10.esm.js';

const FilterMenuPopover = /*#__PURE__*/forwardRef(function FilterMenuPopover(props, forwardedRef) {
  const {
    popoverProps
  } = useComboboxPopover(props, forwardedRef);
  return /*#__PURE__*/jsx(Popover, {
    ...popoverProps
  });
});
FilterMenuPopover.displayName = 'FilterMenuPopover';

export { FilterMenuPopover };
