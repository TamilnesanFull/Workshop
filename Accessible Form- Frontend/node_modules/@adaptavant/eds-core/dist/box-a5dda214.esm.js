import { forwardRef } from 'react';
import { g as getStyleProps } from './index-dd10a77e.esm.js';
import { jsx } from 'react/jsx-runtime';

/**
 * Box
 *
 * @description A generic unstyled component that can be used in place of any standard element.
 */

const Box = /*#__PURE__*/forwardRef(function Box({
  as: Comp = 'div',
  className = '',
  classNames = {},
  style = {},
  styles = {},
  ...consumerProps
}, forwardedRef) {
  const styleProps = getStyleProps({
    className,
    classNames,
    style,
    styles
  });
  return /*#__PURE__*/jsx(Comp, {
    ...consumerProps,
    ref: forwardedRef,
    ...styleProps('root'),
    "data-eds-component": true
  });
});
Box.displayName = 'Box';

export { Box as B };
