'use client';
import { useContext, createContext } from 'react';
import { jsx } from 'react/jsx-runtime';

const BrandStylesContext = /*#__PURE__*/createContext(undefined);
const BrandStylesProvider = ({
  children,
  value
}) => {
  // TODO: merge with parent
  // const parent = useContext(BrandStylesContext);

  return /*#__PURE__*/jsx(BrandStylesContext.Provider, {
    value: value,
    children: children
  });
};
function useBrandStyles() {
  const context = useContext(BrandStylesContext);
  if (!context) {
    throw new Error('useBrandStyles must be used within a BrandStylesProvider');
  }
  return context;
}

export { BrandStylesProvider, useBrandStyles };
