import { t as tw } from './tw-c6a9a210.esm.js';

// ----------------------------- ComboboxListbox -----------------------------

const getComboboxListboxStyles = () => ({
  root: tw`
		outline-none
		overflow-y-auto
	`
});

// ----------------------------- ComboboxItemStyles -----------------------------

const getComboboxItemStyles = selectedOption => ({
  root: tw`
		${selectedOption ? 'px-1' : 'px-2'}
		gap-1
		items-start
	`
});

export { getComboboxListboxStyles as a, getComboboxItemStyles as g };
